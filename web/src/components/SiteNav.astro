---
import navData from "../data/navigation.json";
import type { NavItem } from "../types/navigation";
import { withBase } from "../lib/nav-url";

const base = import.meta.env.BASE_URL;
const items = navData as NavItem[];
---

<div class="site-nav-wrap">
  <button
    type="button"
    class="site-nav-burger"
    id="site-nav-burger"
    aria-expanded="false"
    aria-controls="site-nav-drawer"
    aria-label="மெனுவைத் திற"
  >
    <span class="site-nav-burger__bar" aria-hidden="true"></span>
    <span class="site-nav-burger__bar" aria-hidden="true"></span>
    <span class="site-nav-burger__bar" aria-hidden="true"></span>
  </button>

  <nav class="site-nav site-nav--desktop" aria-label="Primary" data-site-nav-desktop>
    <ul class="site-nav__root">
      {
        items.map((item) => (
          <li
            class:list={["site-nav__tier1", item.children?.length ? "site-nav__tier1--has-sub" : ""]}
            data-nav-item
          >
            <span class="site-nav__row">
              <a class="site-nav__link" href={withBase(base, item.path)}>
                {item.label}
              </a>
              {item.children?.length ? (
                <button
                  type="button"
                  class="site-nav__caret"
                  aria-expanded="false"
                  aria-haspopup="true"
                  aria-label={`${item.label} — உட்பிரிவுகள்`}
                  data-nav-caret
                >
                  <span aria-hidden="true">▾</span>
                </button>
              ) : null}
            </span>
            {item.children?.length ? (
              <ul class="site-nav__sub" role="list">
                {item.children.map((ch) => (
                  <li>
                    <a href={withBase(base, ch.path)}>{ch.label}</a>
                  </li>
                ))}
              </ul>
            ) : null}
          </li>
        ))
      }
    </ul>
  </nav>

  <div class="site-nav-backdrop" id="site-nav-backdrop" hidden data-nav-backdrop></div>
  <div class="site-nav-drawer" id="site-nav-drawer" hidden data-nav-drawer>
    <div class="site-nav-drawer__head">
      <span class="site-nav-drawer__title">பிரிவுகள்</span>
      <button type="button" class="site-nav-drawer__close" id="site-nav-close" aria-label="மூடு">
        ×
      </button>
    </div>
    <nav aria-label="Mobile primary">
      <ul class="site-nav-mobile">
        {
          items.map((item) => (
            <li class="site-nav-mobile__item">
              {item.children?.length ? (
                <details class="site-nav-mobile__details">
                  <summary class="site-nav-mobile__summary">
                    <span>{item.label}</span>
                    <span class="site-nav-mobile__chev" aria-hidden="true"></span>
                  </summary>
                  <ul class="site-nav-mobile__sub">
                    <li>
                      <a href={withBase(base, item.path)}>பிரிவு முகப்பு</a>
                    </li>
                    {item.children.map((ch) => (
                      <li>
                        <a href={withBase(base, ch.path)}>{ch.label}</a>
                      </li>
                    ))}
                  </ul>
                </details>
              ) : (
                <a class="site-nav-mobile__solo" href={withBase(base, item.path)}>
                  {item.label}
                </a>
              )}
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</div>

<script>
  (function () {
    const burger = document.getElementById("site-nav-burger");
    const drawer = document.querySelector("[data-nav-drawer]");
    const backdrop = document.querySelector("[data-nav-backdrop]");
    const closeBtn = document.getElementById("site-nav-close");
    if (!burger || !drawer || !backdrop) return;

    function openDrawer() {
      drawer.hidden = false;
      backdrop.hidden = false;
      burger.setAttribute("aria-expanded", "true");
      document.body.classList.add("site-nav-drawer-open");
    }
    function closeDrawer() {
      drawer.hidden = true;
      backdrop.hidden = true;
      burger.setAttribute("aria-expanded", "false");
      document.body.classList.remove("site-nav-drawer-open");
    }

    burger.addEventListener("click", () => {
      if (drawer.hidden) openDrawer();
      else closeDrawer();
    });
    backdrop.addEventListener("click", closeDrawer);
    closeBtn?.addEventListener("click", closeDrawer);
    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape") closeDrawer();
    });

    document.querySelectorAll("[data-nav-drawer] a").forEach((a) => {
      a.addEventListener("click", () => closeDrawer());
    });

    document.querySelectorAll("[data-nav-caret]").forEach((btn) => {
      btn.addEventListener("click", (e) => {
        e.preventDefault();
        e.stopPropagation();
        const li = btn.closest("[data-nav-item]");
        if (!li) return;
        const open = li.classList.toggle("is-open");
        btn.setAttribute("aria-expanded", open ? "true" : "false");
      });
    });

    document.addEventListener("click", (e) => {
      const t = e.target;
      if (!(t instanceof Element)) return;
      if (t.closest("[data-nav-item]") || t.closest("[data-nav-caret]")) return;
      document.querySelectorAll("[data-nav-item].is-open").forEach((li) => {
        li.classList.remove("is-open");
        li.querySelector("[data-nav-caret]")?.setAttribute("aria-expanded", "false");
      });
    });
  })();
</script>

<style>
  .site-nav-wrap {
    display: flex;
    align-items: stretch;
    justify-content: flex-end;
    gap: 0.5rem;
    width: 100%;
    min-width: 0;
    flex: 1;
    overflow: visible;
  }

  .site-nav-burger {
    display: none;
    flex-direction: column;
    justify-content: center;
    align-self: center;
    gap: 5px;
    width: 2.75rem;
    height: 2.75rem;
    margin: 0.35rem 0.5rem 0.35rem 0.35rem;
    padding: 0.5rem;
    border: 1px solid #c4c4c4;
    background: #f3f3f3;
    border-radius: 3px;
    cursor: pointer;
    flex-shrink: 0;
  }
  .site-nav-burger__bar {
    display: block;
    height: 2px;
    background: #1a1a1a;
    border-radius: 1px;
  }

  .site-nav--desktop {
    flex: 1;
    min-width: 0;
    font-family: "Noto Sans Tamil", "Open Sans", sans-serif;
    font-size: 0.98rem;
    font-weight: 600;
    align-self: stretch;
    display: flex;
    align-items: center;
    overflow: visible;
  }

  /**
   * Do NOT use overflow-x: auto here — it clips `position:absolute` dropdowns.
   * Tabs wrap on small widths instead.
   */
  .site-nav__root {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    list-style: none;
    margin: 0;
    padding: 0 0.25rem 0 0;
    overflow: visible;
    gap: 0;
  }

  .site-nav__tier1 {
    position: relative;
    display: inline-flex;
    align-items: center;
    flex-shrink: 0;
    margin: 0;
  }

  /** Keeps hover while moving pointer from tab into the panel (no 1px “dead air”). */
  .site-nav__tier1--has-sub::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    top: 100%;
    height: 0.45rem;
    z-index: 119;
    background: transparent;
  }

  .site-nav__root > .site-nav__tier1 + .site-nav__tier1::before {
    content: "";
    display: block;
    width: 5px;
    height: 5px;
    margin: 0 0.65rem;
    background: #0582ca;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .site-nav__row {
    display: inline-flex;
    align-items: center;
    gap: 0.05rem;
    min-height: 2.85rem;
    padding: 0 0.15rem 0 0.25rem;
  }

  .site-nav__tier1--has-sub:hover .site-nav__row,
  .site-nav__tier1--has-sub:focus-within .site-nav__row {
    background: #efefef;
    border-radius: 2px 2px 0 0;
  }

  .site-nav__link {
    display: inline-flex;
    align-items: center;
    padding: 0.5rem 0.35rem 0.5rem 0.55rem;
    color: #1a1a1a !important;
    text-decoration: none !important;
    white-space: nowrap;
    letter-spacing: 0.01em;
  }
  .site-nav__link:hover,
  .site-nav__link:focus-visible {
    color: #0582ca !important;
    outline: none;
  }

  .site-nav__caret {
    border: 0;
    background: transparent;
    color: #333;
    padding: 0.35rem 0.45rem 0.35rem 0.1rem;
    cursor: pointer;
    line-height: 1;
    font-size: 0.55rem;
    align-self: center;
  }
  .site-nav__caret:hover,
  .site-nav__caret:focus-visible {
    color: #0582ca;
    outline: none;
  }

  .site-nav__sub {
    position: absolute;
    top: calc(100% - 1px);
    left: 0;
    min-width: 15.5rem;
    margin: 0;
    padding: 0.4rem 0;
    list-style: none;
    background: #f5f5f5;
    border: 1px solid #4a4a4a;
    box-shadow: 2px 8px 20px rgba(0, 0, 0, 0.18);
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transform: translateY(-2px);
    transition:
      opacity 0.12s ease,
      transform 0.12s ease,
      visibility 0.12s;
    z-index: 120;
  }
  .site-nav__sub a {
    display: block;
    padding: 0.5rem 1rem;
    color: #111 !important;
    text-decoration: none !important;
    font-family: "Noto Sans Tamil", "Open Sans", sans-serif;
    font-size: 0.92rem;
    font-weight: 500;
    white-space: nowrap;
    border-bottom: 1px solid #e0e0e0;
  }
  .site-nav__sub li:last-child a {
    border-bottom: 0;
  }
  .site-nav__sub a:hover {
    background: #e8e8e8;
    color: #0582ca !important;
  }

  .site-nav__tier1--has-sub:hover .site-nav__sub,
  .site-nav__tier1--has-sub:focus-within .site-nav__sub,
  .site-nav__tier1--has-sub.is-open .site-nav__sub {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    transform: translateY(0);
  }

  .site-nav-backdrop {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.45);
    z-index: 80;
  }
  .site-nav-drawer {
    position: fixed;
    top: 0;
    right: 0;
    width: min(20rem, 92vw);
    height: 100vh;
    background: #fff;
    z-index: 90;
    box-shadow: -4px 0 24px rgba(0, 0, 0, 0.15);
    overflow-y: auto;
    overscroll-behavior: contain;
    scrollbar-width: thin;
    scrollbar-color: #c4c4c4 transparent;
    display: flex;
    flex-direction: column;
  }

  .site-nav-drawer::-webkit-scrollbar {
    width: 6px;
  }
  .site-nav-drawer::-webkit-scrollbar-thumb {
    background: #c4c4c4;
    border-radius: 4px;
  }
  .site-nav-drawer::-webkit-scrollbar-track {
    background: transparent;
  }
  .site-nav-drawer__head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.75rem 1rem;
    border-bottom: 1px solid #e5e5e5;
    background: #fafafa;
  }
  .site-nav-drawer__title {
    font-family: Oswald, "Noto Sans Tamil", sans-serif;
    font-weight: 600;
    color: #252422;
  }
  .site-nav-drawer__close {
    border: 0;
    background: transparent;
    font-size: 1.75rem;
    line-height: 1;
    cursor: pointer;
    color: #333;
    padding: 0.25rem;
  }

  .site-nav-mobile {
    list-style: none;
    margin: 0;
    padding: 0.5rem 0 2rem;
  }
  .site-nav-mobile__item {
    border-bottom: 1px solid #eee;
  }
  .site-nav-mobile__solo {
    display: block;
    padding: 0.85rem 1rem;
    color: #0582ca !important;
    text-decoration: none !important;
    font-weight: 600;
    font-size: 0.95rem;
  }
  .site-nav-mobile__details {
    border: 0;
  }
  .site-nav-mobile__summary {
    list-style: none;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.65rem 1rem;
    cursor: pointer;
    font-weight: 600;
    color: #1a1a1a;
  }
  .site-nav-mobile__summary::-webkit-details-marker {
    display: none;
  }
  .site-nav-mobile__chev::before {
    content: "▾";
    color: #666;
    font-size: 0.75rem;
  }
  .site-nav-mobile__sub {
    list-style: none;
    margin: 0;
    padding: 0 0 0.5rem 0.5rem;
    background: #fafafa;
  }
  .site-nav-mobile__sub a {
    display: block;
    padding: 0.5rem 0.75rem;
    color: #0582ca !important;
    text-decoration: none !important;
    font-size: 0.92rem;
  }

  @media (max-width: 767px) {
    .site-nav-burger {
      display: flex;
    }
    .site-nav--desktop {
      display: none;
    }
  }
</style>

<style is:global>
  body.site-nav-drawer-open {
    overflow: hidden;
  }
</style>
